import { render, screen } from '@testing-library/react'
import Searcher from './Searcher'

describe.skip('Searcher Component', () => {
  // it('renders correctly', () => {
  //   render(<Searcher />)
  //   const el = screen.getByText('')
  //   expect(el).toBeInTheDocument()
  // })
})
